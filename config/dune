(executable
 (name discover)
 (modules Discover)
 (libraries str dune-configurator))

(rule
 (targets
  camlp5-flags.cfg
  gt-flags.cfg
  gt-flags-native.cfg
  logger-flags.cfg
  stublibs-dir.cfg
  instrumentalization.cfg
  package-doc.cfg)
 (action
  (run ./discover.exe -all-flags)))
